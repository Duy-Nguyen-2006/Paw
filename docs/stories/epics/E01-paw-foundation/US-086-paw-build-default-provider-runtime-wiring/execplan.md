
# Execution Plan

1. Keep the composed provider executor as the default build runtime path.
2. Preserve provider-unavailable fail-closed handling.
3. Add tests that isolate `ENV_AGENT_DIR` during default-provider execution.
4. Validate with focused tests and repository checks.
